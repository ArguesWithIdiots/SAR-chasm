{
  "type": "object",
  "additionalProperties": false,
  "required": ["overall_status", "summary", "categories", "disclaimer"],
  "properties": {
    "overall_status": {
      "type": "string",
      "enum": ["pass", "flag"]
    },
    "summary": {
      "type": "string",
      "minLength": 1
    },
    "categories": {
      "type": "array",
      "minItems": 5,
      "maxItems": 5,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["category", "status", "rationale"],
        "properties": {
          "category": {
            "type": "string",
            "enum": [
              "5 W's Coverage",
              "Typology Language",
              "Specificity",
              "Internal Consistency",
              "Length & Density"
            ]
          },
          "status": {
            "type": "string",
            "enum": ["pass", "flag"]
          },
          "rationale": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "disclaimer": {
      "type": "string",
      "minLength": 1
    }
  }
}
