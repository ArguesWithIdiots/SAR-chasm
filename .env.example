# Create a copy named .env and add your own OpenAI Platform API key.
OPENAI_API_KEY=your-api-key-here

# Review model default. Change this to another model available to your API project.
OPENAI_MODEL=gpt-5.6-terra

# The server intentionally binds to localhost by default.
HOST=127.0.0.1
PORT=8000
